// 키 바인딩을 이 파일에 넣어서 기본값을 덮어씁니다.
[
    //컴파일
    { "key": "ctrl+shift+b", "command": "workbench.action.tasks.build" },
    
    //실행
    { "key": "ctrl+f5", "command": "workbench.action.tasks.test" }
]